// Define a custom annotation class called View
class View {
  // A string property to hold the provider name
  final String provider;

  // Constructor for the View annotation, which requires the provider name
  const View({required this.provider});
}
